import YCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
YCheckboxButton.install = function(Vue) {
  Vue.component(YCheckboxButton.name, YCheckboxButton);
};

export default YCheckboxButton;
